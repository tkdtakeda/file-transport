# PDFファイル名ルール管理および自動仕分けツール 仕様書
2
 
3
## 1. 目的
4
 
5
指定フォルダ内に置かれたPDFファイルを対象に、ファイル名に含まれるキーワード、年月情報、文書種別、設備名など
その他の行を表示する
4. 運用フロー
4.1 ルール編集時
Plain Text
1
edit_rules.bat を実行
2
↓
3
PythonローカルWebサーバー起動
4
↓
5
ブラウザでルール編集画面
その他の行を表示する
4.2 PDF移動時
Plain Text
1
run_sort.bat を実行
2
↓
3
Pythonが rules.json を読み込む
4
↓
5
同一フォルダ内の
その他の行を表示する
5. 前提条件
OSはWindowsを想定する。
Python 3.x がインストール済みであること。
Python標準ライブラリのみで実装する。
対象ファイルは、原則として pdf_sorter_app.py と同じフォルダに存在するPDFファイルとする。
サブフォルダ内のPDFは初期仕様では対象外とする。
保存先はローカルフォルダ、ネットワーク同期フォルダ、またはクラウド同期フォルダを想定する。
クラウド同期完了までは本ツールの保証範囲外とする。
本ツールはローカルファイル操作の完了のみを保証対象とする。
外部ネットワークへファイル名、ルール、保存先情報を送信しない。
6. 主要機能
6.1 ルール管理機能
ブラウザ画面でPDF仕分けルールを追加、編集、削除できること。
6.1.1 ルール項目
JSON
1
{
2
"enabled": true,
3
"priority": 1,
4
"name": "Equipment A Calibration
その他の行を表示する
6.1.2 ルール項目定義
項目	型	必須	説明enabled	boolean	必須	ルールを有効にするか
priority	number	必須	判定順。小さい値を優先
name	string	必須	ルール表示名
extension	string	任意	対象拡張子。通常は .pdf
contains_all	string[]	任意	ファイル名にすべて含む必要があるキーワード
contains_any	string[]	任意	ファイル名にいずれか含めばよいキーワード
not_contains	string[]	任意	ファイル名に含まれてはいけないキーワード
destination_subfolder	string	必須	共通保存先配下の保存先サブフォルダ
6.2 共通設定管理機能
rules.json に共通設定を保持する。
6.2.1 設定例
JSON
1
{
2
"common_base": "{BASE_FOLDER}"
その他の行を表示する
6.2.2 共通設定項目定義
項目	型	推奨値	説明common_base	string	{BASE_FOLDER}	仕分け先の共通親フォルダ
unknown_folder	string	_unknown	ルール未一致ファイルの退避先
log_folder	string	_log	ログCSV保存先
use_month_folder	boolean	true	年月フォルダを使用するか
month_folder_format	string	YYYY-MM	年月フォルダ名の形式
month_source	string	filename	年月の取得元
month_fallback	string	unknown_month	年月が取れない場合の動作
duplicate_mode	string	evacuate	同名ファイル時の処理
evacuation_folder_prefix	string	避難用	避難先フォルダ名の接頭語
forbid_overwrite	boolean	true	上書きを禁止する
move_strategy	string	copy_verify_delete	安全移動方式
verify_after_move	boolean	true	移動後に存在確認する
check_file_stable	boolean	true	処理前にファイル安定確認する
min_file_age_seconds	number	10	更新直後ファイルを処理しない秒数
backup_rules_on_save	boolean	true	rules.json保存時にバックアップする
max_path_length	number	240	パス長上限目安
rules	array	[]	仕分けルール一覧
7. PDF仕分け仕様
7.1 対象ファイル
対象は以下を満たすファイルとする。
Plain Text
1
1. pdf_sorter_app.py と同じフォルダに存在する
2
2. ファイルで
その他の行を表示する
7.1.1 除外対象
以下は処理対象外とする。
Plain Text
1
~$sample.pdf
2
sample.tmp
3
sample.part
4
 
その他の行を表示する
7.2 処理対象リスト固定
処理開始時点で対象PDF一覧を固定する。
処理中に追加されたPDFは次回実行時に処理する。
Plain Text
1
At startup, create fixed target file list.
2
Do not process
その他の行を表示する
7.3 ルール判定順
Plain Text
1
1. rules.json を読み込む
2
2. enabled = true のルールのみ
その他の行を表示する
7.4 ルール一致条件
以下をすべて満たした場合に一致とする。
Plain Text
1
1. ファイル拡張子が rule.extension と一致する
2
2.
その他の行を表示する
8. 年月フォルダ仕様
8.1 年月フォルダ構成
use_month_folder = true の場合、保存先は以下の構成とする。
Plain Text
1
{common_base}
2
└─ {destination_
その他の行を表示する
例:
Plain Text
1
{BASE_FOLDER}\EquipmentA\2026-06\EquipmentA_CalibrationRecord_2026.06
その他の行を表示する
8.2 年月抽出元
初期仕様では、年月はファイル名から抽出する。
JSON
1
"month_source": "filename"
その他の行を表示する
8.3 対応する年月表記
以下の表記を認識する。
Plain Text
1
2026.6
2
2026.06
3
2026-6
4
2026-06
5
2026_6
6
2026
その他の行を表示する
出力フォルダ名は以下に統一する。
Plain Text
1
YYYY-MM
その他の行を表示する
変換例:
Plain Text
1
2026.6 -> 2026-06
2
2026.06 -> 2026
その他の行を表示する
8.4 年月が取得できない場合
month_fallback に従って処理する。
設定値	動作current_month	実行日の年月を使用
unknown_month	_年月不明 フォルダへ保存
error	移動せずエラー
推奨値:
JSON
1
"month_fallback": "unknown_month"
その他の行を表示する
理由:
Plain Text
1
実行月へ誤保存するより、年月不明として退避した方が安全なため。
その他の行を表示する
8.5 年月候補が複数ある場合
ファイル名から年月候補が複数検出された場合、通常保存先へは保存しない。
以下のフォルダへ退避する。
Plain Text
1
_年月確認要
その他の行を表示する
結果コードは以下とする。
Plain Text
1
MONTH_AMBIGUOUS
その他の行を表示する
8.6 年月フォルダ自動作成
保存先の年月フォルダが存在しない場合、自動作成する。
Plain Text
1
if destination_month_folder does not exist:
2
create destination
その他の行を表示する
作成に失敗した場合は、元ファイルを移動しない。
結果コード:
Plain Text
1
ERROR_CREATE_FOLDER
その他の行を表示する
9. 同名ファイル時の仕様
9.1 基本方針
保存予定先に同名ファイルが存在する場合、既存ファイルを上書きしてはならない。
また、通常保存先では自動連番保存しない。
理由:
Plain Text
1
通常保存先で自動連番保存すると、重複に気づきにくくなるため。
その他の行を表示する
9.2 同名ファイル時の動作
duplicate_mode = evacuate の場合、以下の動作とする。
Plain Text
1
同名ファイルあり
2
↓
3
通常保存先には保存しない
4
↓
5
警告メッセージを出す
6
↓
7
保存予定年月
その他の行を表示する
9.3 避難先フォルダ
避難先フォルダ名:
Plain Text
1
避難用_YYYYMMDD
その他の行を表示する
例:
Plain Text
1
避難用_20260815
その他の行を表示する
避難先作成場所:
Plain Text
1
{common_base}
2
└─ {destination_
その他の行を表示する
9.4 避難先にも同名ファイルがある場合
避難先フォルダ内にも同名ファイルがある場合のみ、連番を付ける。
Plain Text
1
file.pdf
2
file_001.pdf
3
file_002.pdf
その他の行を表示する
10. 安全移動仕様
10.1 移動方式
安全優先の初期値は以下とする。
JSON
1
"move_strategy": "copy_verify_delete
その他の行を表示する
10.1.1 move
通常の移動。
Plain Text
1
shutil.move(source, destination)
その他の行を表示する
10.1.2 copy_verify_delete
安全重視の移動。
Plain Text
1
1. copy source to destination
2
2. verify destination
その他の行を表示する
10.1.3 copy_only
コピーのみ。元ファイルを残す。
10.2 推奨方式
初期実装では以下を推奨する。
JSON
1
"move_strategy": "copy_verify_delete"
その他の行を表示する
理由:
Plain Text
1
移動途中の例外による紛失リスクを低減できるため。
その他の行を表示する
11. ファイル安定確認
11.1 リスク
PDFがコピー中、スキャン出力中、同期中の場合、未完成ファイルを処理する可能性がある。
11.2 対策
check_file_stable = true の場合、以下を確認する。
Plain Text
1
1. ファイル更新時刻が min_file_age_seconds
その他の行を表示する
不安定な場合は処理しない。
結果コード:
Plain Text
1
SKIP_FILE_NOT_STABLE
その他の行を表示する
12. ロックファイルによる二重起動防止
12.1 リスク
複数の run_sort.bat が同時実行されると、同じファイルを二重処理する可能性がある。
12.2 対策
処理開始時にロックファイルを作成する。
Plain Text
1
.sorter.lock
その他の行を表示する
ロックファイルが既に存在する場合は、処理を中断する。
結果コード:
Plain Text
1
ERROR_LOCKED
その他の行を表示する
処理終了時、正常終了、異常終了に関わらずロックファイルを削除する。
13. rules.json保存仕様
13.1 リスク
保存中に中断されると rules.json が破損する可能性がある。
13.2 保存手順
Plain Text
1
1. 受信したJSONをバリデーションする
2
2. 現在の rules.json を rules
その他の行を表示する
14. パス安全性チェック
14.1 長すぎるパス
移動前にフルパス長を確認する。
JSON
1
"max_path_length": 240
その他の行を表示する
上限を超える場合は処理しない。
結果コード:
Plain Text
1
ERROR_PATH_TOO_LONG
その他の行を表示する
14.2 禁止文字
保存先サブフォルダ名にWindows禁止文字が含まれる場合、ルール保存時または実行時にエラーとする。
禁止文字:
Plain Text
1
< > : " / \ | ? *
その他の行を表示する
注意:
Plain Text
1
サブフォルダ階層を許可する場合、\ はパス区切りとして扱う設計も可能。
2
ただし初期実装では、destination_subfolder は単一フォルダ名または相対パスとして明確に扱うこと。
その他の行を表示する
15. 判定不能ファイル
15.1 ルール未一致
ルールに一致しないPDFは以下へ移動する。
Plain Text
1
{script_folder}\{unknown_folder}\original_file.pdf
その他の行を表示する
結果コード:
Plain Text
1
UNKNOWN
その他の行を表示する
15.2 注意表示
処理完了時に以下を表示する。
Plain Text
1
移動成功件数
2
避難保存件数
3
判定不能件数
4
エラー件数
5
スキップ件数
その他の行を表示する
16. ログ仕様
16.1 ログ保存先
Plain Text
1
{script_folder}\{log_folder}\move_log.csv
その他の行を表示する
16.2 文字コード
Excelで開きやすいように、UTF-8 BOM付きで保存する。
Plain Text
1
utf-8-sig
その他の行を表示する
16.3 ログ列
CSV
1
日時,
2
ファイル名,
3
抽出年月,
4
判定ルール,
5
移動元,
6
通常保存予定先,
7
実際の保存先,
8
結果,
9
メッセージ
その他の行を表示する
16.4 ログ例
通常移動成功:
CSV
1
2026-08-15 22:18:00,EquipmentA_CalibrationRecord_2026.06.pdf,2026-06,Equipment A Calibration Record,{SCRIPT_FOLDER}\file.pdf,{BASE_FOLDER}\EquipmentA\2026-06\file.pdf,{BASE_FOLDER}\EquipmentA\2026-06\file.pdf,MOVED,
その他の行を表示する
同名ファイル避難:
CSV
1
2026-08-15 22:18:00,EquipmentA_CalibrationRecord_2026.06.pdf,2026-06,Equipment A Calibration Record,{SCRIPT_FOLDER}\file.pdf,{BASE_FOLDER}\EquipmentA\2026-06\file.pdf,{BASE_FOLDER}\EquipmentA\2026-06\避難用_20260815\file.pdf,DUPLICATE_EVACUATED,同名ファイルが存在したため避難保存
その他の行を表示する
年月不明:
CSV
1
2026-08-15 22:18:00,EquipmentA_CalibrationRecord.pdf,,Equipment A Calibration Record,{SCRIPT_FOLDER}\file.pdf,{BASE_FOLDER}\EquipmentA\_年月不明\file.pdf,{BASE_FOLDER}\EquipmentA\_年月不明\file.pdf,MONTH_UNKNOWN,ファイル名から年月を取得できません
その他の行を表示する
年月複数:
CSV
1
2026-08-15 22:18:00,EquipmentA_CalibrationRecord_2026.06_next_2026.12.pdf,,Equipment A Calibration Record,{SCRIPT_FOLDER}\file.pdf,{BASE_FOLDER}\EquipmentA\_年月確認要\file.pdf,{BASE_FOLDER}\EquipmentA\_年月確認要\file.pdf,MONTH_AMBIGUOUS,年月候補が複数あります
その他の行を表示する
17. 結果コード一覧
コード	意味MOVED	通常移動成功
COPIED_AND_DELETED	コピー検証後に元削除成功
DUPLICATE_EVACUATED	同名ファイルがあり避難保存
UNKNOWN	ルール未一致
MONTH_UNKNOWN	年月不明
MONTH_AMBIGUOUS	年月候補が複数
SKIP	設定によりスキップ
SKIP_FILE_NOT_STABLE	ファイルが安定していないためスキップ
ERROR_CREATE_FOLDER	フォルダ作成失敗
ERROR_MOVE_FAILED	移動失敗
ERROR_COPY_FAILED	コピー失敗
ERROR_DELETE_SOURCE_FAILED	元ファイル削除失敗
ERROR_VERIFY_FAILED	移動後検証失敗
ERROR_PATH_TOO_LONG	パス長すぎ
ERROR_RULE_INVALID	ルール不正
ERROR_LOCKED	他プロセス実行中
ERROR_RULES_JSON_INVALID	rules.json が不正
ERROR_FORBIDDEN_PATH	保存先パスが不正
18. ローカルWebサーバー仕様
18.1 待ち受け
ローカルPC内のみで動作すること。
Plain Text
1
host = 127.0.0.1
2
port = 8765
その他の行を表示する
18.2 セキュリティ方針
Plain Text
1
1. 外部ネットワークからアクセスさせない
2
2. 外部URLへデータ送信しない
3
3. ファイル名、保存先、ルール情報はローカル内で処理する
4
4. APIはローカル利用のみを前提とする
その他の行を表示する
19. API仕様
19.1 GET /
ルール編集画面HTMLを返す。
レスポンス:
Plain Text
1
Content-Type: text/html; charset=utf-8
その他の行を表示する
19.2 GET /api/rules
現在の rules.json を読み込んで返す。
レスポンス例:
JSON
1
{
2
"common_base": "{BASE_FOLDER}",
3
"unknown_folder": "_unknown",
4
"log_folder": "_log",
5
"use_month_folder": true,
6
"month_folder_format": "YYYY-MM",
7
"month_source": "filename",
8
"month_fallback": "unknown_month",
9
"duplicate_mode": "evacuate",
10
"evacuation_folder_prefix": "避難用",
11
"forbid_overwrite": true,
12
"move_strategy": "copy_verify_delete",
13
"verify_after_move": true,
14
"check_file_stable": true,
15
"min_file_age_seconds": 10,
16
"backup_rules_on_save": true,
17
"max_path_length": 240,
18
"rules": []
19
}
その他の行を表示する
19.3 POST /api/rules
リクエストボディのJSONを rules.json に安全保存する。
処理:
Plain Text
1
1. JSON受信
2
2. バリデーション
3
3. 既存rules.jsonをバックアップ
4
4. tmpファイルへ保存
5
5. rules.jsonへ置換
6
6. 保存結果を返却
その他の行を表示する
レスポンス例:
JSON
1
{
2
"ok": true,
3
"message": "saved"
4
}
その他の行を表示する
19.4 POST /api/preview
リクエストボディのルール設定を使い、PDF移動予定先を返す。実ファイルは移動しない。
レスポンス例:
JSON
1
{
2
"ok": true,
3
"items": [
4
{
5
"file_name": "EquipmentA_CalibrationRecord_2026.06.pdf",
6
"extracted_month": "2026-06",
7
"rule_name": "Equipment A Calibration Record",
8
"source": "{SCRIPT_FOLDER}\\EquipmentA_CalibrationRecord_2026.06.pdf",
9
"planned_destination": "{BASE_FOLDER}\\EquipmentA\\2026-06\\EquipmentA_CalibrationRecord_2026.06.pdf",
10
"actual_destination": "",
11
"result": "MATCH",
12
"message": ""
13
}
14
]
15
}
その他の行を表示する
19.5 POST /api/sort
リクエストボディのルール設定を保存後、PDF移動を実行する。
レスポンス例:
JSON
1
{
2
"ok": true,
3
"items": [
4
{
5
"file_name": "EquipmentA_CalibrationRecord_2026.06.pdf",
6
"extracted_month": "2026-06",
7
"rule_name": "Equipment A Calibration Record",
8
"source": "{SCRIPT_FOLDER}\\EquipmentA_CalibrationRecord_2026.06.pdf",
9
"planned_destination": "{BASE_FOLDER}\\EquipmentA\\2026-06\\EquipmentA_CalibrationRecord_2026.06.pdf",
10
"actual_destination": "{BASE_FOLDER}\\EquipmentA\\2026-06\\EquipmentA_CalibrationRecord_2026.06.pdf",
11
"result": "MOVED",
12
"message": ""
13
}
14
]
15
}
その他の行を表示する
20. 画面仕様
20.1 画面タイトル
Plain Text
1
PDF自動仕分け ルール管理
その他の行を表示する
20.2 操作ボタン
ボタン	処理rules.json に保存	画面上の設定を保存
移動プレビュー	移動予定先を表示
PDF移動を実行	画面上の設定を保存後、PDF移動を実行
再読み込み	rules.json を再読み込み
20.3 共通設定エリア
以下の入力欄を持つ。
Plain Text
1
共通保存先フォルダ
2
判定不能フォルダ名
3
ログフォルダ名
4
年月フォルダを使用
5
年月が取れない場合の動作
6
同名ファイル時の処理
7
安全移動方式
8
ファイル安定確認
9
最小ファイル経過秒数
10
最大パス長
その他の行を表示する
20.4 ルール一覧エリア
各ルールに以下の入力欄を表示する。
Plain Text
1
有効
2
ルール名
3
優先順位
4
拡張子
5
すべて含むキーワード
6
いずれか含むキーワード
7
含んではいけないキーワード
8
保存先サブフォルダ
その他の行を表示する
20.5 プレビュー結果エリア
表形式で表示する。
Plain Text
1
ファイル名
2
抽出年月
3
判定ルール
4
通常保存予定先
5
実際の保存先予定
6
結果
7
メッセージ
その他の行を表示する
21. バッチファイル仕様
21.1 edit_rules.bat
ルール編集画面を起動する。
BAT
1
@echo off
2
cd /d "%~dp0"
3
 
4
echo Starting rule editor.
5
echo Press Ctrl + C in this window to stop the local server.
6
echo.
7
 
8
py pdf_sorter_app.py server
9
 
10
pause
その他の行を表示する
Pythonランチャー py が使えない環境では python に置き換える。
21.2 run_sort.bat
PDF移動を実行する。
BAT
1
@echo off
2
cd /d "%~dp0"
3
 
4
echo Starting PDF sorting.
5
echo.
6
 
7
py pdf_sorter_app.py sort
8
 
9
echo.
10
echo Finished.
11
pause
その他の行を表示する
Pythonランチャー py が使えない環境では python に置き換える。
22. コマンドライン仕様
pdf_sorter_app.py は以下のモードを持つ。
Plain Text
1
server ルール編集画面を起動
2
sort rules.json に従いPDF移動を実行
3
preview rules.json に従い移動予定先をコンソール表示
その他の行を表示する
実行例:
BAT
1
py pdf_sorter_app.py server
2
py pdf_sorter_app.py sort
3
py pdf_sorter_app.py preview
その他の行を表示する
23. 初期 rules.json 例
JSON
1
{
2
"common_base": "{BASE_FOLDER}",
3
"unknown_folder": "_unknown",
4
"log_folder": "_log",
5
"use_month_folder": true,
6
"month_folder_format": "YYYY-MM",
7
"month_source": "filename",
8
"month_fallback": "unknown_month",
9
"duplicate_mode": "evacuate",
10
"evacuation_folder_prefix": "避難用",
11
"forbid_overwrite": true,
12
"move_strategy": "copy_verify_delete",
13
"verify_after_move": true,
14
"check_file_stable": true,
15
"min_file_age_seconds": 10,
16
"backup_rules_on_save": true,
17
"max_path_length": 240,
18
"rules": [
19
{
20
"enabled": true,
21
"priority": 1,
22
"name": "Equipment A Calibration Record",
23
"extension": ".pdf",
24
"contains_all": [
25
"EquipmentA",
26
"CalibrationRecord"
27
],
28
"contains_any": [],
29
"not_contains": [],
30
"destination_subfolder": "EquipmentA"
31
}
32
]
33
}
その他の行を表示する
24. 判定例
24.1 通常一致
ルール:
JSON
1
{
2
"enabled": true,
3
"priority": 1,
4
"name": "Equipment A Calibration Record",
5
"extension": ".pdf",
6
"contains_all": ["EquipmentA", "CalibrationRecord"],
7
"contains_any": [],
8
"not_contains": [],
9
"destination_subfolder": "EquipmentA"
10
}
その他の行を表示する
ファイル名:
Plain Text
1
EquipmentA_CalibrationRecord_2026.06.pdf
その他の行を表示する
保存先:
Plain Text
1
{BASE_FOLDER}\EquipmentA\2026-06\EquipmentA_CalibrationRecord_2026.06.pdf
その他の行を表示する
24.2 ルール未一致
ファイル名:
Plain Text
1
EquipmentA_InspectionRecord_2026.06.pdf
その他の行を表示する
結果:
Plain Text
1
{SCRIPT_FOLDER}\_unknown\EquipmentA_InspectionRecord_2026.06.pdf
その他の行を表示する
結果コード:
Plain Text
1
UNKNOWN
その他の行を表示する
24.3 年月不明
ファイル名:
Plain Text
1
EquipmentA_CalibrationRecord.pdf
その他の行を表示する
結果:
Plain Text
1
{BASE_FOLDER}\EquipmentA\_年月不明\EquipmentA_CalibrationRecord.pdf
その他の行を表示する
結果コード:
Plain Text
1
MONTH_UNKNOWN
その他の行を表示する
24.4 年月候補複数
ファイル名:
Plain Text
1
EquipmentA_CalibrationRecord_2026.06_next_2026.12.pdf
その他の行を表示する
結果:
Plain Text
1
{BASE_FOLDER}\EquipmentA\_年月確認要\EquipmentA_CalibrationRecord_2026.06_next_2026.12.pdf
その他の行を表示する
結果コード:
Plain Text
1
MONTH_AMBIGUOUS
その他の行を表示する
24.5 同名ファイルあり
保存予定先に以下が既に存在する場合:
Plain Text
1
{BASE_FOLDER}\EquipmentA\2026-06\EquipmentA_CalibrationRecord_2026.06.pdf
その他の行を表示する
新規ファイルは以下に保存する。
Plain Text
1
{BASE_FOLDER}\EquipmentA\2026-06\避難用_20260815\EquipmentA_CalibrationRecord_2026.06.pdf
その他の行を表示する
結果コード:
Plain Text
1
DUPLICATE_EVACUATED
その他の行を表示する
25. エラー処理
25.1 rules.json が存在しない場合
初期設定で自動作成する。
作成に失敗した場合はエラー表示し、処理を中断する。
25.2 rules.json が不正な場合
処理を中断する。
ファイル移動は行わない。
エラー内容を表示する。
結果コード:
Plain Text
1
ERROR_RULES_JSON_INVALID
その他の行を表示する
25.3 保存先フォルダ作成失敗
元ファイルは移動しない。
ログに記録する。
他ファイルの処理は継続する。
結果コード:
Plain Text
1
ERROR_CREATE_FOLDER
その他の行を表示する
25.4 コピー失敗
元ファイルは削除しない。
ログに記録する。
他ファイルの処理は継続する。
結果コード:
Plain Text
1
ERROR_COPY_FAILED
その他の行を表示する
25.5 移動後検証失敗
ログに記録する。
元ファイル、移動先ファイルの存在状態をメッセージへ出力する。
結果コード:
Plain Text
1
ERROR_VERIFY_FAILED
その他の行を表示する
25.6 元ファイル削除失敗
copy_verify_delete 方式でコピー成功後、元ファイル削除に失敗した場合はログに記録する。
結果コード:
Plain Text
1
ERROR_DELETE_SOURCE_FAILED
その他の行を表示する
26. 事故防止要件
以下は必須要件とする。
同名ファイルの上書き禁止
通常保存先での自動連番保存禁止
同名ファイル時は避難用フォルダへ保存
年月フォルダ自動作成
年月不明は _年月不明 へ退避
年月候補複数は _年月確認要 へ退避
移動後の存在確認
コピー検証後に元削除する安全移動方式
処理中ファイルを避けるためのファイル安定確認
ロックファイルによる二重起動防止
rules.json のバックアップ保存
rules.json の一時ファイル経由保存
ログCSVのUTF-8 BOM保存
パス長チェック
禁止文字チェック
処理対象PDFリストの固定
サブフォルダ内PDFを初期対象外とする
一時ファイルを除外する
27. 非機能要件
27.1 セキュリティ
ローカルWebサーバーは 127.0.0.1 のみで待ち受ける。
外部ネットワークへ公開しない。
外部APIへデータ送信しない。
ファイル名、パス、ルール情報はローカルPC内で処理する。
27.2 保守性
新規ルール追加時にPythonコード変更を不要にする。
仕分け条件は rules.json のみで管理する。
画面からルールを追加、編集、削除できること。
将来的にHTML、JavaScript、Pythonを分離できる構成が望ましい。
27.3 拡張性
将来的に以下へ拡張可能な設計とする。
Plain Text
1
PDF本文テキストの読み取りによる判定
2
OCR結果による判定
3
Excel台帳への結果記録
4
複数ルールセット切り替え
5
サブフォルダ再帰処理
6
移動ではなくコピーするモード
7
保存先の絶対パス指定
8
match_mode による一致方式変更
9
正規表現による判定
その他の行を表示する
28. 実装方針
28.1 Python
標準ライブラリのみで実装する。
使用候補:
Python
1
pathlib
2
json
3
shutil
4
csv
5
datetime
6
http.server
7
urllib.parse
8
webbrowser
9
threading
10
sys
11
os
12
time
13
re
その他の行を表示する
28.2 フロントエンド
HTML、CSS、JavaScriptのみで実装する。
使用候補:
Plain Text
1
fetch API
2
JSON.stringify
3
DOM操作
4
table表示
その他の行を表示する
フレームワークは使用しない。
29. 受け入れ条件
以下を満たせば完了とする。
edit_rules.bat 実行でブラウザにルール編集画面が表示される。
ルール編集画面でルールを追加、編集、削除できる。
画面上の保存ボタンで rules.json が直接上書きされる。
rules.json 保存時にバックアップが作成される。
rules.json は一時ファイル経由で安全保存される。
run_sort.bat 実行で同一フォルダ内のPDFが仕分けされる。
ファイル名がルールに一致するPDFは指定フォルダへ保存される。
年月フォルダが存在しない場合、自動作成される。
年月が取得できない場合、_年月不明 へ保存される。
年月候補が複数ある場合、_年月確認要 へ保存される。
保存先に同名ファイルがある場合、上書きされない。
同名ファイルは 避難用_YYYYMMDD フォルダへ保存される。
移動後に移動先ファイルの存在確認が行われる。
copy_verify_delete 方式で安全に移動できる。
ファイル安定確認により、コピー中ファイルを処理しない。
二重起動時はロックファイルにより処理を中断する。
実行結果がUTF-8 BOM付きCSVログへ出力される。
処理完了時に成功件数、避難件数、判定不能件数、エラー件数、スキップ件数が表示される。
社内固有情報をコードに直書きせず、設定ファイルで変更できる。
外部ネットワークへデータ送信しない。
30. 外部AIへの実装依頼文
以下の内容でPythonアプリを実装してください。
Plain Text
1
Windowsローカル環境で動作するPDF自動仕分けツールを作成してください。
2
 
3
要件:
4
1. Python標準ライブラリのみで実装する。
5
2. pdf_sorter_app.py、edit_rules.bat、run_sort.bat、rules.json の構成にする。
6
3. edit_rules.bat で PythonローカルWebサーバーを起動し、ブラウザにルール編集画面を表示する。
7
4. ルール編集画面では、共通保存先、判定不能フォルダ、ログフォルダ、年月フォルダ設定、同名ファイル時の処理、安全移動方式、複数の仕分けルールを編集できるようにする。
8
5. 画面の保存ボタンでPython経由により rules.json を直接上書き保存する。ダウンロード方式にはしない。
9
6. rules.json 保存時は既存ファイルをバックアップし、一時ファイル経由で安全に置換する。
10
7. run_sort.bat で同一フォルダ内のPDFを rules.json に従って移動する。
11
8. ルール判定は、拡張子、すべて含むキーワード、いずれか含むキーワード、含んではいけないキーワードで行う。
12
9. ルールは priority 昇順で評価し、最初に一致したルールを採用する。
13
10. 一致しないPDFは判定不能フォルダへ移動する。
14
11. 保存先は common_base + destination_subfolder の下に、年月フォルダ YYYY-MM を作成して保存する。
15
12. 年月はPDFファイル名から抽出する。
16
13. 対応する年月表記は YYYY.M、YYYY.MM、YYYY-M、YYYY-MM、YYYY_M、YYYY_MM、YYYYMM とする。
17
14. 年月フォルダが存在しない場合は自動作成する。
18
15. ファイル名から年月が取得できない場合は _年月不明 フォルダへ保存する。
19
16. 年月候補が複数ある場合は _年月確認要 フォルダへ保存する。
20
17. 保存先に同名ファイルがある場合、通常保存先には上書きしない。
21
18. 同名ファイルがある場合は、保存予定年月フォルダ内に 避難用_YYYYMMDD フォルダを作成し、そこへ保存する。
22
19. 避難用フォルダ内にも同名ファイルがある場合のみ _001、_002 の連番を付ける。
23
20. 通常保存先での自動連番保存は禁止する。
24
21. 実行前にロックファイルを作成し、二重起動を防止する。
25
22. 移動前にファイルサイズが安定しているか確認する。
26
23. 移動方式として copy_verify_delete を実装する。
27
24. コピー後に移動先ファイルの存在とサイズを確認し、確認後に元ファイルを削除する。
28
25. すべての結果を UTF-8 BOM付きCSVログへ記録する。
29
26. ブラウザ画面に移動プレビュー機能を付ける。
30
27. ローカルWebサーバーは 127.0.0.1 のみで待ち受ける。
31
28. 社内固有情報はコードに含めず、rules.json の設定値として扱う。
32
29. 外部ネットワークへデータ送信しない。
その他の行を表示する
31. 匿名化ルール
外部AIに渡す際は、以下の情報を含めないこと。
区分	外部AIに渡さない情報	置換例実フォルダパス	実在する会社フォルダ、部署フォルダ	{BASE_FOLDER}
部署名	実在部署名	DepartmentA
設備名	必要に応じて匿名化	EquipmentA
実ファイル名	実ファイル名	EquipmentA_CalibrationRecord_YYYY.MM.pdf
個人名	担当者名	UserA
会社名	会社名	CompanyA
32. 最終処理イメージ
Plain Text
1
PDFを同一フォルダへ置く
2
↓
3
run_sort.bat 実行
4
↓
5
ロックファイル作成
6
↓
7
rules.json 読み込み
8
↓
9
PDF一覧を固定
10
↓
11
ファイル安定確認
12
↓
13
ファイル名ルール判定
14
↓
15
年月抽出
16
↓
17
年月フォルダ決定
18
↓
19
年月フォルダ作成
20
↓
21
同名チェック
22
↓
23
同名なし:
24
copy_verify_delete で保存
25
同名あり:
26
避難用_YYYYMMDD 作成
27
避難用フォルダへ保存
28
年月不明:
29
_年月不明 へ保存
30
年月複数:
31
_年月確認要 へ保存
32
↓
33
ログ出力
34
↓
35
集計表示
36
↓
37
ロックファイル削除
その他の行を表示する
33. 重要な設計方針
本ツールでは、利便性よりもファイルの保全を優先する。
特に以下を厳守する。
Plain Text
1
1. 既存ファイルを上書きしない
2
2. 通常保存先で自動連番保存しない
3
3. 同名ファイルは避難用フォルダへ退避する
4
4. 年月不明、年月複数は通常保存しない
5
5. 移動はコピー、検証、元削除の順で行う
6
6. ログを必ず残す
7
7. ルールファイルはバックアップしてから保存する
その他の行を表示する

## 説明

- **Markdown形式を採用**しました。外部AIに渡す際に、見出し、表、JSON例、バッチ例、処理フローが崩れにくいためです。
- **社内情報は匿名化**しています。実パス、会社名、部署名、設備名、実ファイル名は `{BASE_FOLDER}` や `EquipmentA` に置換しています。
- **事故防止を強化**しています。上書き禁止、同名時の避難保存、年月不明退避、年月複数退避、二重起動防止、コピー検証後削除まで仕様に含めています。